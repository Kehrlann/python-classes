class Polynomial:
    def __init__(self, *coefs):
        # Notez que "coeff" est une liste de coefficients
        # REMPLACEZ LE CODE CI-DESSOUS
        for i, coeff in enumerate(coefs):
            print(f"coeff {i}: {coeff}")
        pass
